interface:
  display_name: "Create Spec"
  short_description: "Turn a grilled Linear brief into a spec"
  default_prompt: "Use $create-spec to replace the grilled Linear brief in this conversation with an implementation-ready specification."
